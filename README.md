# minzo-services
Backend services for the Minzo website

## Installing dependencies
Run ```poetry install --sync --all-extras --with dev,test,coverage``` at the root of the project.

## Running development micro-services
Run ```poetry run <service name>``` for example: ```poetry run minzo```
